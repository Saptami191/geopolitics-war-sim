import { Modes2State } from '../types';

export function processModes2Tick(modes2State: Modes2State, modesState: any, worldState: any, defconState: any, sigintState: any, cyberState: any, ciaState: any, diplomaticState: any, blocState: any, mirrorState: any, sanctionsState: any, oversightState: any, currentTick: number): Modes2State {
    return modes2State;
}

export const PREDEFINED_SCENARIOS = {};
